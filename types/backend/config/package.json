{
    "name": "@ems/types-backend-config",
    "version": "0.0.0",
    "type": "module",
    "exports": "./src/index.ts",
    "scripts": {
        "check": "tsc --noEmit",
        "lint": "prettier --check . && eslint .",
        "format": "prettier --write ."
    }
}
