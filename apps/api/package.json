{
    "name": "api",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node --watch --watch-preserve-output --env-file=.env --env-file=.env.secret src/index.js",
        "lint": "prettier --check . && eslint .",
        "format": "prettier --write ."
    },
    "dependencies": {
        "fastify": "^5.2.0"
    },
    "devDependencies": {
        "pino-pretty": "^13.0.0"
    }
}
