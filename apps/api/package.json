{
    "name": "@ems/app-api",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "check": "tsc --noEmit --project jsconfig.json",
        "dev": "node --watch --watch-preserve-output --env-file=.env.dev --env-file=.env.secret src/index.js",
        "lint": "prettier --check . && eslint .",
        "format": "prettier --write .",
        "test": "vitest run --passWithNoTests",
        "test:unit": "vitest --passWithNoTests"
    },
    "dependencies": {
        "@ems/database": "*",
        "@ems/domain-backend-auth": "*",
        "fastify": "^5.2.0"
    },
    "devDependencies": {
        "@ems/types-backend-config": "*",
        "pino-pretty": "^13.0.0"
    }
}
