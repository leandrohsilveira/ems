{
    "name": "@ems/domain-backend-auth",
    "version": "0.0.0",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.d.ts",
            "default": "./src/index.js"
        }
    },
    "scripts": {
        "check": "tsc --noEmit --project jsconfig.json",
        "lint": "prettier --check . && eslint .",
        "format": "prettier --write .",
        "test": "vitest run",
        "test:unit": "vitest --run"
    },
    "dependencies": {
        "@ems/database": "*",
        "@ems/types-backend-auth": "*",
        "bcrypt": "^6.0.0",
        "fastify": "^5.2.0",
        "fastify-plugin": "^5.1.0",
        "jsonwebtoken": "^9.0.2"
    },
    "devDependencies": {
        "@types/bcrypt": "^5.0.2",
        "@types/jsonwebtoken": "^9.0.5"
    }
}
